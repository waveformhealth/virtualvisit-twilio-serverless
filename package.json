{
  "name": "virtualvisit-twilio-serverless",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "devDependencies": {
    "twilio": "^3.43.1",
    "twilio-run": "^2.5.0"
  },
  "engines": {
    "node": "10"
  }
}
